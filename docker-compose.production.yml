version: '3'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    volumes:
      - pg_data:/var/lib/postgresql/data
    env_file: .env
  backend:
    image: sashamehaev/foodgram_backend
    env_file: .env
    volumes:
    - static:/backend_static
    - media:/app/media
  frontend:
    image: sashamehaev/foodgram_frontend
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    image: sashamehaev/foodgram_gateway
    ports:
      - 10000:80
    volumes:
      - static:/staticfiles/
      - media:/media/